import '../print.css';

/** [1.5] הדפסה מרוכזת (בחירה מרובה) — אותו עיקרון כמו print/[doc] של הזמנה בודדת. */
export default function BulkPrintLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
