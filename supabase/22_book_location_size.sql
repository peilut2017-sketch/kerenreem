-- ============================================================================
-- מכון קרן רא"ם — מיקום מלאי וגודל פיזי לספר
-- להרצה אחרי 21_contact_topics_fields.sql
-- ============================================================================
-- שני שדות טקסט חופשי בלשונית "מסחר" בטופס הספר: מיקום המלאי (למשל
-- "מדף A3" / "מחסן 2") — מידע תפעולי לצוות לאיתור הספר פיזית, לא מוצג
-- באתר הציבורי — וגודל פיזי (למשל "17x24 ס״מ").
-- ============================================================================

alter table books add column if not exists stock_location text;
alter table books add column if not exists physical_size text;
