-- ============================================================================
-- מכון קרן רא"ם — מחבר כטקסט חופשי
-- להרצה אחרי 18_page_views.sql
-- ============================================================================
-- שני שדות רשות על ספר: שם מחבר כטקסט חופשי, לצד שיוך המחבר הקיים
-- (author_id). כשהם מלאים הם גוברים על שם המחבר המשויך בכל מקום שהספר
-- מוצג — כרטיס, עמוד הספר וכו' — בלי קישור לעמוד מחבר, ובלי ליצור או
-- לגעת ברשומת מחבר קיימת. מיועד למקרים שבהם אין טעם ביצירת רשומת מחבר
-- מלאה: עורך אורח, "מכון קרן רא״ם", מחבר לא ידוע וכיו״ב.
-- ============================================================================

alter table books add column if not exists author_name_he text;
alter table books add column if not exists author_name_en text;
